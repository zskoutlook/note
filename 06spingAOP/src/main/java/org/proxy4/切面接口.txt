proxy3的案例：给代理类、目标类上了代理接口，把目标类对象作为代理类内部的属性，在代理类方法内部调用目标类方法来实现代理，可以灵活更改目标类对象
             缺点：代理类的切面功能是写死的

改进：如果想让代理类的切面功能也能灵活更改？
     再上一个切面的接口，切面类去实现该接口，再代理类中让切面类对象作为代理类的属性，在代理类方法内部调用切面类方法来实现切面功能，可以灵活更改切面类对象
     缺点：虽然能够切换切面对象，但只能上一个切面对象，如果需要上不知道多个切面对象，咋办？---》让代理对象被代理，传入要增加的切面对象
